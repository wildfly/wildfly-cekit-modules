schema_version: 1
name: jboss.container.wildfly.launch.admin
version: '1.0'
description: Configures the admin user and associated management realm
execute:
  - script: configure.sh
    user: '185'
envs:
  - name: "PORT_OFFSET"
    example: "100"
    description: If defined changes the port offset.
